##### Learn rust in a docker container

- Steps
  - [Clone]() the repo
  - run `docker-compose build`
  - run `docker-compose run app`

  Now, a debain docker container is ready to play with Rust at your wish

